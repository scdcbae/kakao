<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hanbit2007.kakao">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".presentation.member.RegistActivity" />
        <activity android:name=".presentation.member.LoginActivity" />
        <activity android:name=".presentation.member.SearchActivity" />
        <activity android:name=".presentation.member.ModifyActivity" />
        <activity android:name=".presentation.member.UnregistActivity" />
        <activity android:name=".presentation.message.ListMsgActivity" />
        <activity android:name=".presentation.message.DeleteMsgActivity" />
        <activity android:name=".presentation.message.SearchMsgActivity" />
        <activity android:name=".presentation.message.UpdateMsgActivity" />
        <activity android:name=".presentation.message.WriteMsgActivity" />
        <activity android:name=".presentation.member.ListActivity" />
        <activity android:name=".presentation.member.DetailActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".presentation.member.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>